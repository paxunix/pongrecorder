- reset the form
- delete a single saved match

- XXX: there is a bug where filling out 2v2 then toggling to 1v1 causes
  an infinite loop when setting state (uses up all memory)
- XXX: bug where toggling from 2v2 to 1v1 doesn't enable the player names
  that are no longer visible"

